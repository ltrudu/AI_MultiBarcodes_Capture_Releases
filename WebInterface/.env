# =========================================================
# AI MultiBarcode Capture - Unified Container Configuration
# =========================================================
#
# This configuration controls the unified multibarcode-webinterface container
# which includes Apache+PHP, MySQL, and phpMyAdmin in a single image.
#
# After modifying this file, restart the services:
#   ./start-services.sh (Linux/macOS)
#   start-services.bat (Windows)
#

# =========================================================
# SERVICE PORTS
# =========================================================

# Web Management System (WMS) Interface Port
# This is the main dashboard where you monitor barcode sessions
# Access: http://localhost:3500
WEB_PORT=3500

# =========================================================
# OPTIONAL SERVICE EXPOSURE
# =========================================================

# Enable phpMyAdmin Interface (set to true to expose)
# Web-based database administration interface
# Access: http://localhost:3500/phpmyadmin (when enabled)
EXPOSE_PHPMYADMIN=false

# MySQL Database Access (always internal only in unified container)
# Direct database access not exposed externally for security
EXPOSE_MYSQL=false

# =========================================================
# DATABASE CONFIGURATION
# =========================================================

# Database Connection Settings
DB_HOST=127.0.0.1
DB_NAME=barcode_wms
DB_USER=wms_user
DB_PASS=wms_password

# MySQL Root Password (for administrative access)
MYSQL_ROOT_PASSWORD=root_password

# =========================================================
# COMMON PORT ALTERNATIVES
# =========================================================
#
# If default ports are in use, try these alternatives:
#
# WEB_PORT=8080        # Default
# WEB_PORT=8090        # Alternative if 8080 is busy
# WEB_PORT=9080        # Alternative if 8090 is busy
#
# PHPMYADMIN_PORT=8081 # Default
# PHPMYADMIN_PORT=8091 # Alternative if 8081 is busy
# PHPMYADMIN_PORT=9081 # Alternative if 8091 is busy
#
# MYSQL_PORT=3306      # Default
# MYSQL_PORT=3307      # Alternative if 3306 is busy
# MYSQL_PORT=3308      # Alternative if 3307 is busy